const Header = () => {
  return (
    <header className="bg-black h-14 w-screen">
      <span className="flex justify-center items-center h-full">
        <h2 className="font-sans text-3xl font-semibold">Flipcost</h2>
      </span>
    </header>
  );
};

export default Header;
